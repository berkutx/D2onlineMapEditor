"""fflib - reusable library for decoding Disciples 2 .ff (MQDB) archives.

Promoted from the de-risk spikes in ``tools/asset-pipeline/spikes`` (probe_ff.py,
probe_colorkey.py). Pure stdlib + Pillow + numpy (3.7 interpreter).
"""
