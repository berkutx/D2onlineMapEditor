# D2 asset pipeline — pinned to the offline 3.7 interpreter on this machine.
# Do NOT pip-install (assume offline); these are already present:
#   python 3.7.3  +  Pillow 6.2.0  +  numpy 1.21.5
Pillow==6.2.0
numpy==1.21.5

# Optional: enables strict JSON-Schema validation in manifest.validate().
# When absent, the pipeline falls back to a builtin draft-07 schema walker that
# validates against packages/asset-manifest/gen/asset-manifest.schema.json.
# jsonschema
