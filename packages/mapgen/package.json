{
  "name": "@d2/mapgen",
  "version": "0.1.0",
  "description": "MarkovJunior recipe runner (vendored MarkovJuniorWeb, MIT) — runs a recipe to a symbol grid, headless. Decode to EditOps lives in @d2/map-edit.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./types.d.ts",
  "exports": {
    ".": {
      "types": "./types.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "types.d.ts"],
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.10",
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "@types/seedrandom": "^3.0.8",
    "esbuild": "^0.24.2"
  }
}
