# pnpm: keep the dependency tree strict so package boundaries (contracts) are enforced
strict-peer-dependencies=false
auto-install-peers=true
prefer-workspace-packages=true
link-workspace-packages=true
